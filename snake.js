

class snake
{

	var dirrEnum = {"up":1, "down":2, "left":3, "right":4};
	
	var path = []; //all coordinates occupied by the snake
	var speed = 15; //int snek sped
	var direction = dirrEnum.up; //current snake direction
	var instanceID = generateUID(); 
	var isAlive = true;
	var thickness = 1;



	function changeDir(){}

	function move(direction, speed, path)
	{

var currDir = changeDir();
var mouthPosition = path[0];
var newMouthPos = 

if (path == dirrEnum.up) {}
	else if (path == dirrEnum.down){}
		else if (path == dirrEnum.right){}
			else {}

	}

}
